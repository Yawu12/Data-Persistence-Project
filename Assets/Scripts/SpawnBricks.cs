using UnityEngine;

public class SpawnBricks : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        MainManager.instance.SpawnBricks();
    }

    // Update is called once per frame
    void Update()
    {
        
    }



}
